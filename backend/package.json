{
  "name": "kleine-musikschule-contact-backend",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for contact form with noreply and autoresponse emails",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "prisma skills sync || exit 0",
    "contract:emit": "prisma contract emit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "resend": "^6.26.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "prisma": "^5.22.0"
  }
}
